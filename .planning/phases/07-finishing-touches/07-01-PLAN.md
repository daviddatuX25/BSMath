---
phase: "07-finishing-touches"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "assets/js/router.js"
  - "views/shell.html"
  - "assets/js/views/dashboard.js"
autonomous: true
requirements: []
must_haves:
  truths:
    - "Program Head cannot see Events nav link in sidebar"
    - "Dashboard header displays 'Welcome [Role] Panel' format"
    - "External resources (Tailwind CDN, Material Symbols, Google Fonts) are documented"
  artifacts:
    - path: "assets/js/router.js"
      provides: "RBAC enforcement for Events route"
      contains: "roles: ['admin', 'dean']"
    - path: "views/shell.html"
      provides: "Sidebar nav HTML with RBAC data attributes"
      contains: "data-roles=\"admin,dean\""
    - path: "assets/js/views/dashboard.js"
      provides: "Dashboard welcome header"
      contains: "Welcome Administrator Panel"
    - path: "docs/EXTERNAL_RESOURCES.md"
      provides: "Documentation of external CDN resources"
      contains: "Tailwind CSS CDN, Material Symbols, Google Fonts"
---

<objective>
Fix 3 gaps identified by Phase 6 verification to ensure full rubric compliance:
1. Remove `program_head` from Events nav access (guide.md specifies Programs, Gallery, Announcements only)
2. Change dashboard header from "Welcome, [name]" to "Welcome [Role] Panel" format
3. Document that Tailwind CDN, Material Symbols, and Google Fonts are external resources
</objective>

<execution_context>
 @$HOME/.claude/get-shit-done/workflows/execute-plan.md
 @$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-presentation-preparation/06-VERIFICATION.md

## Gap Summary from VERIFICATION.md

The Phase 6 verification identified these gaps requiring Phase 7 fixes:
1. RBAC: Program Head has Events nav access but guide.md specifies only Programs, Gallery, Announcements
2. Dashboard header: "Welcome, [name]" instead of "Welcome [Role] Panel" format
3. Source code constraint: External resources need documentation

## Current Code State

**router.js lines 47-51 (Events nav item):**
```javascript
  {
    label: 'Events',
    icon:  'event',
    hash:  '/events',
    roles: ['admin', 'dean', 'program_head'],  // <-- must remove program_head
  },
```

**router.js lines 134-143 (Events route guard):**
```javascript
  '/events': {
    roles:  ['admin', 'dean'],  // already correct (no program_head)
    ...
  },
```

**shell.html lines 92-100 (Events nav link in sidebar):**
```html
      <a
        href="#/events"
        class="nav-item nav-item--inactive"
        data-roles="admin,dean,program_head"  <!-- must remove program_head -->
        ...
```

**dashboard.js lines 31-38 (Welcome header):**
```javascript
            <!-- Welcome header -->
            <div>
                <h2 class="text-2xl font-serif font-semibold text-stone-900">
                    Welcome, ${user?.name ?? 'User'}
                </h2>
                <p class="text-stone-500 text-sm mt-0.5 capitalize">
                    ${(user?.role ?? 'guest').replace('_', ' ')}
                </p>
            </div>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Events RBAC in router.js NAV_ITEMS</name>
  <files>assets/js/router.js</files>
  <action>
    Change line 50 from:
      roles: ['admin', 'dean', 'program_head'],
    To:
      roles: ['admin', 'dean'],

    The Events route guard (lines 134-143) already correctly excludes program_head.
    Only the NAV_ITEMS sidebar definition needs this fix.
  </action>
  <verify>
    <automated>grep -n "roles.*admin.*dean.*program_head" assets/js/router.js | grep -v Events || echo "No program_head in Events roles found"</automated>
  </verify>
  <done>router.js line 50: Events nav item roles is ['admin', 'dean']</done>
</task>

<task type="auto">
  <name>Task 2: Fix Events RBAC data-roles in shell.html</name>
  <files>views/shell.html</files>
  <action>
    Change line 95 from:
      data-roles="admin,dean,program_head"
    To:
      data-roles="admin,dean"

    This is the sidebar nav link that router.js applyRbacToNav() filters.
  </action>
  <verify>
    <automated>grep -n 'data-roles="admin,dean"' views/shell.html | grep events || echo "Events data-roles still has program_head"</automated>
  </verify>
  <done>shell.html line 95: Events nav link data-roles is "admin,dean"</done>
</task>

<task type="auto">
  <name>Task 3: Fix dashboard welcome header format</name>
  <files>assets/js/views/dashboard.js</files>
  <action>
    Replace lines 31-38 from:
            <!-- Welcome header -->
            <div>
                <h2 class="text-2xl font-serif font-semibold text-stone-900">
                    Welcome, ${user?.name ?? 'User'}
                </h2>
                <p class="text-stone-500 text-sm mt-0.5 capitalize">
                    ${(user?.role ?? 'guest').replace('_', ' ')}
                </p>
            </div>

    To:
            <!-- Welcome header -->
            <div>
                <h2 class="text-2xl font-serif font-semibold text-stone-900">
                    Welcome ${formatRole(user?.role ?? 'guest')} Panel
                </h2>
            </div>

    The formatRole() function is already defined in router.js (lines 418-422) and handles
    role key to label mapping (admin -> Administrator, dean -> Dean / Principal,
    program_head -> Program Head).
  </action>
  <verify>
    <automated>grep -n "Welcome.*Panel" assets/js/views/dashboard.js</automated>
  </verify>
  <done>dashboard.js: Welcome header shows "Welcome [Role] Panel" format</done>
</task>

<task type="auto">
  <name>Task 4: Document external resources</name>
  <files>docs/EXTERNAL_RESOURCES.md</files>
  <action>
    Create docs/EXTERNAL_RESOURCES.md documenting the three external CDN resources used:

    ## External Resources Added Beyond ws4.2 Source Code

    This project uses three external resources that are NOT part of the ws4.2 Bootstrap starter source code. These were added to support the admin SPA functionality.

    ### 1. Tailwind CSS CDN
    - **URL:** https://cdn.tailwindcss.com
    - **Purpose:** Utility-first CSS framework for styling
    - **Justification:** Provides responsive grid, flexbox, and color system matching mock.html fidelity
    - **Location in codebase:** index.html line 9

    ### 2. Google Fonts (Newsreader + Work Sans)
    - **URL:** https://fonts.googleapis.com/css2?family=Newsreader:opsz,wght@6..72,400;6..72,600;6..72,700&family=Work+Sans:wght@300;400;500;600;700&display=swap
    - **Purpose:** Typography system (Newsreader for headlines, Work Sans for body)
    - **Justification:** Required for design-token fidelity with mock.html
    - **Location in codebase:** index.html lines 12-15

    ### 3. Material Symbols Outlined
    - **URL:** https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap
    - **Purpose:** Icon font for navigation and UI elements
    - **Justification:** Required for icon system matching mock.html
    - **Location in codebase:** index.html lines 18-21

    ### Note on External Resources
    All three resources are loaded via CDN at runtime. The application requires internet
    connectivity for full styling and icon rendering. Offline usage will have degraded
    visuals but core functionality remains intact (Tailwind CDN has a fallback warning
    in index.html line 36-39).
  </action>
  <verify>
    <automated>test -f docs/EXTERNAL_RESOURCES.md && echo "EXTERNAL_RESOURCES.md exists"</automated>
  </verify>
  <done>docs/EXTERNAL_RESOURCES.md created with all three external resources documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {e.g., client→API} | {untrusted input crosses here} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | External resource URLs | accept | URLs are public CDN endpoints, no sensitive data |
</threat_model>

<verification>
## Phase 7 Gap Closure Verification Checklist

After executing this plan, verify each gap is closed:

### Gap 1: RBAC Events Access
- [ ] Login as program_head (program_head@bsmath.test / password123)
- [ ] Navigate to dashboard - confirm no "Events" nav link in sidebar
- [ ] Direct navigation to #/events should redirect to dashboard

### Gap 2: Dashboard Header Format
- [ ] Login as admin - dashboard shows "Welcome Administrator Panel"
- [ ] Login as dean - dashboard shows "Welcome Dean / Principal Panel"
- [ ] Login as program_head - dashboard shows "Welcome Program Head Panel"

### Gap 3: External Resources Documentation
- [ ] docs/EXTERNAL_RESOURCES.md exists
- [ ] Contains documentation for Tailwind CDN, Google Fonts, Material Symbols
- [ ] File paths and line numbers in index.html are accurate
</verification>

<success_criteria>
- program_head cannot see Events in sidebar nav
- Dashboard header displays "Welcome [Role] Panel" for all roles
- docs/EXTERNAL_RESOURCES.md documents all three external CDN resources
- All 4 tasks complete with automated verification passing
</success_criteria>

<output>
After completion, create `.planning/phases/07-finishing-touches/07-01-SUMMARY.md`
</output>
