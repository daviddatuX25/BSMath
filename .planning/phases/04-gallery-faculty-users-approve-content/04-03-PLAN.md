---
plan: "04-03"
phase: 4
wave: 3
title: "Phase 4 Demo Checkpoint"
objective: "Human-verified demo of all Phase 4 modules across all 3 roles. Gallery upload works, users are managed, Dean approves content."
depends_on:
  - "04-02"
files_modified: []
autonomous: false
requirements:
  - R5
  - R6
  - R7
  - R8
  - R9
  - R10
  - R11
  - R12
  - R13
type: execute
must_haves:
  truths:
    - "Admin can create a new Program Head user and upload a gallery image"
    - "Program Head can upload gallery images and create pending announcements"
    - "Dean can see pending content under Approve Content and approve/reject"
    - "Dean CANNOT access Gallery, Faculty, or Users (redirected to dashboard)"
    - "Program Head CANNOT access Faculty, Users, or Approvals (redirected to dashboard)"
  artifacts: []
---

<objective>
Verify all Phase 4 modules work end-to-end across all 3 roles. This is a human-testing checkpoint — no code to write, just browser testing.
</objective>

<execution_context>
@C:\Users\sarmi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\sarmi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="manual">
<name>Task 1: Test Admin role — Gallery, Faculty, Users, Approvals visibility</name>
<read_first>
  .planning/REQUIREMENTS.md
</read_first>
<files>none</files>
<action>
Log in as admin@bsmath.test (password: admin123) and test these flows:

**Gallery module:**
1. Click "Gallery" in sidebar → see image grid with 3 seeded images
2. Click "Upload Image" → modal opens with file input
3. Select a JPG/PNG image → fill title "Test Upload" → click Upload
4. Toast shows "Gallery image uploaded" → new image appears in grid
5. Hover image → edit/delete buttons appear
6. Click edit → change title to "Edited Upload" → Save → toast shows updated
7. Click delete → confirm → toast shows deleted → image removed from grid

**Faculty module:**
1. Click "Faculty" in sidebar → see table with 4 seeded members
2. Click "Add Faculty" → fill name "Dr. Test Professor" → position "Instructor" → status "Active" → Add
3. Toast shows "Faculty member added" → new row appears
4. Edit "Dr. Test Professor" → change position to "Assistant Professor" → Save
5. Delete "Dr. Test Professor" → confirm → toast shows deleted

**Users module:**
1. Click "Users" in sidebar → see table with 3+ users (NO password column)
2. Click "Add User" → fill name "Test PH", email "testph@bsmath.test", password "test123", role "Program Head" → Add
3. Toast shows "User added" → new user appears with "Program Head" badge
4. Edit "Test PH" → leave password blank → change role to "Dean" → Save → toast shows updated
5. Try to delete your own admin account → should show error "Cannot delete your own account"

**Approvals module:**
1. Click "Approvals" in sidebar (if visible — admin can view pending items)
2. See list of pending announcements + events
3. Note: Admin CANNOT approve/reject (only Dean can) — if buttons appear, they should return 403

**RBAC verification for Admin:**
- Sidebar shows: Dashboard, Programs, Announcements, Events, News, Gallery, Faculty, Users, Approvals, Profile
</action>
<acceptance_criteria>
  - Admin can upload, edit, and delete gallery images
  - Admin can add, edit, and delete faculty members
  - Admin can add, edit, and delete users (with password handling)
  - Admin cannot delete own account
  - Users table does NOT show password column
  - Admin sidebar shows all modules
</acceptance_criteria>
<done>Admin role verified — full CRUD access to Gallery, Faculty, Users.</done>
</task>

<task type="manual">
<name>Task 2: Test Program Head role — Gallery access, Faculty/Users/Approvals blocked</name>
<read_first>
  .planning/REQUIREMENTS.md
</read_first>
<files>none</files>
<action>
Log in as program_head@bsmath.test (password: head123) and test:

**Gallery module:**
1. Click "Gallery" in sidebar → see image grid
2. Upload a new image → works fine
3. Edit and delete existing images → works fine

**Faculty module:**
1. Try to click "Faculty" in sidebar → should NOT be visible
2. Manually navigate to #/faculty → should redirect to #/dashboard

**Users module:**
1. Try to click "Users" in sidebar → should NOT be visible
2. Manually navigate to #/users → should redirect to #/dashboard

**Approvals module:**
1. Try to click "Approvals" in sidebar → should NOT be visible
2. Manually navigate to #/approvals → should redirect to #/dashboard

**Announcements (to create pending items for Dean):**
1. Navigate to Announcements → create a new announcement titled "Pending Test"
2. Announcement should appear with status "pending" badge

**RBAC verification for Program Head:**
- Sidebar shows: Dashboard, Programs, Announcements, Gallery, Profile
- Sidebar does NOT show: News, Events, Faculty, Users, Approvals
</action>
<acceptance_criteria>
  - Program Head can access Gallery (upload, edit, delete)
  - Program Head CANNOT access Faculty (sidebar hidden + route redirect)
  - Program Head CANNOT access Users (sidebar hidden + route redirect)
  - Program Head CANNOT access Approvals (sidebar hidden + route redirect)
  - Program Head can create a pending announcement for Dean approval
</acceptance_criteria>
<done>Program Head role verified — Gallery works, other modules blocked.</done>
</task>

<task type="manual">
<name>Task 3: Test Dean role — Approve Content workflow, Gallery/Faculty/Users blocked</name>
<read_first>
  .planning/REQUIREMENTS.md
</read_first>
<files>none</files>
<action>
Log in as dean@bsmath.test (password: dean123) and test:

**Approve Content module:**
1. Click "Approvals" in sidebar → see pending announcements + events
2. Tab filter buttons visible: "All Pending", "Announcements", "Events"
3. Click "Announcements" tab → only pending announcements shown
4. Click "Events" tab → only pending events shown
5. Click "All Pending" tab → both shown
6. Find the "Pending Test" announcement created by Program Head
7. Click "Approve" → toast shows "Announcement approved" → item disappears from list
8. If there are pending events, click "Reject" on one → toast shows "Event rejected" → item disappears

**Gallery module:**
1. Try to click "Gallery" in sidebar → should NOT be visible
2. Manually navigate to #/gallery → should redirect to #/dashboard

**Faculty module:**
1. Try to click "Faculty" in sidebar → should NOT be visible
2. Manually navigate to #/faculty → should redirect to #/dashboard

**Users module:**
1. Try to click "Users" in sidebar → should NOT be visible
2. Manually navigate to #/users → should redirect to #/dashboard

**Verify approved announcement:**
1. Log out, log back in as admin
2. Navigate to Announcements → find the "Pending Test" announcement
3. Status badge should now show "approved" (emerald green) instead of "pending" (amber)

**RBAC verification for Dean:**
- Sidebar shows: Dashboard, Announcements, Events, Approvals, Profile
- Sidebar does NOT show: Programs, News, Gallery, Faculty, Users
</action>
<acceptance_criteria>
  - Dean can access Approvals and see pending items
  - Dean can Approve announcements → status changes to approved
  - Dean can Reject events → status changes to rejected
  - Tab filter works (All/Announcements/Events)
  - After approval, announcement shows "approved" badge when viewed as admin
  - Dean CANNOT access Gallery (sidebar hidden + route redirect)
  - Dean CANNOT access Faculty (sidebar hidden + route redirect)
  - Dean CANNOT access Users (sidebar hidden + route redirect)
</acceptance_criteria>
<done>Dean role verified — approval workflow works, other modules blocked.</done>
</task>

</tasks>

<verification>
All 3 tasks are manual browser testing. No automated verification — human checks each flow.
</verification>

<success_criteria>
- All Phase 4 modules work end-to-end across all 3 roles
- RBAC is correct: each role sees only their permitted modules
- Gallery file upload, edit, delete all work
- Users CRUD with password hashing works
- Dean approval workflow changes status and removes items from pending list
- No page reloads during any navigation
</success_criteria>

<output>
After completion, create `.planning/phases/04-gallery-faculty-users-approve-content/04-03-SUMMARY.md`
</output>